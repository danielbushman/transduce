"use strict";
var UNDEFINED;

var assert = require("assert");
var N = require("../dist/N.js");

describe('card game', function () {
  it("should accept input parameter for number of players", ()=> {
    // function game(num_players)
  });
  it("should deal 5 cards to each of the players", ()=> {

    game.players.map(())

  });
});
